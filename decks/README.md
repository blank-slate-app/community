# decks/

One folder per published deck, named `Title - Author` — exactly what the
app's **Publish Deck…** produces. Each folder is listed in the root
`index.json`. See `../CONTRIBUTING.md` to submit yours.
